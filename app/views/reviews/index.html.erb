<% @reviews.each do |review| %>
  <%= tag.div id: "review-#{review.id}" do %>
  <%= review.title %>
  <%= review.description %>
  Rating: <%= review.rating %>/5
  <%= link_to 'Edit Review', edit_review_path(review) %>
  <%= link_to 'Delete Review', review_path(review), method: :delete %>
  <% end %>
<% end %>
